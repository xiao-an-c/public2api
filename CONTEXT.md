# 领域词汇

本仓库的单一真源。代码标识符、面板文案、讨论里的说法都对齐这里。
新词先加进本文件，再进代码。

## 渠道（Channel）

**是什么**：用户可见的**维护空间**。账号、策略、密钥作用域、用量统计、维护任务
全部挂在它下面。目前三个：`wbp-cn`、`wbp-global`、`grok`。

**边界**：
- 渠道的划分依据是**账号体系**，不是技术实现。Grok 的三条入口共用一份账号，
  所以是一个渠道；WorkBuddy 国内与国际是两套账号，所以是两个。
- 渠道是**结构**，不是筛选条件。底子 `workbuddy2api-panel` 里的 `realm` 只在查询时
  过滤，提升成一等公民是本仓库相对底子最重要的变化。

**判据**（两条都满足才拆）：① 账号体系是否独立——凭据能不能通用、额度是不是各算；
② 运维策略是否要能分开配——国内号要签到、Grok 要挂代理。

## 账号类型（Kind）

**是什么**：渠道内的凭证 / 上游形态，决定登录方式、凭据结构与上游协议。
目前四种：`wbp`、`grok_web`、`grok_build`、`grok_console`。

**边界**：
- 一个账号类型**可以**属于多个渠道。`wbp` 同时属于 `wbp-cn` 与 `wbp-global`：
  两边是同一套 OAuth 流程、同一套凭据结构、同一个私有协议，只差端点——
  而端点属于渠道配置。
- 「这个账号属于哪个渠道」由 `accounts.channel` 列**单值**回答，不从账号类型反推。

**判据**：账号类型是**代码分支的边界**。若某差异需要写 `if kind == ...`，
它就该是一种新账号类型；若它只是配置不同（端点、超时），它该留在渠道配置里。

## 能力（Capability）

**是什么**：账号类型声明的、可被程序消费的行为承诺。目前 12 项，四类——
接入（`account.oauth` / `account.device` / `account.credential` / `account.refresh`）、
额度（`quota.query`）、维护（`ops.keepalive` / `ops.checkin` / `ops.tasks` / `ops.egress`）、
业务（`chat` / `media` / `search`）。

**边界**：
- 能力挂在**账号类型**上，不挂在渠道上。渠道的能力集合是它的账号类型的并集
  （`Channel.Capabilities()`）——派生值，不是独立字段。所以「渠道声明了某能力、
  但没有任何账号类型支持它」在结构上无法表达。
- 能力**不是**菜单项。菜单是能力的派生（`Channel.Menu()`），只有部分能力带菜单标签。

**为什么它是核心杠杆**：面板二级菜单、账号导入方式、定时任务调度、API 能力探测
全部由它派生。

## 漂移（Drift）

**是什么**：渠道声明与适配器实现不一致。两个方向都算：

| 方向 | 表现 | 后果 |
| --- | --- | --- |
| 声明了、没实现 | 空头承诺 | 面板渲染出一个点了没反应的按钮 |
| 实现了、没声明 | 隐性能力 | 功能在，但没有任何入口能到达它 |

**边界**：漂移**不是**未接入。整条链路还没写是**进度**（`Registry.Unimplemented`），
不是不一致。混在一起会让骨架阶段满屏噪音，把真正的漂移淹掉。

**为什么值得单独命名**：漂移不报错、不崩，只是让某个能力悄悄失效——这是最难在
测试里发现的一类 bug。所以它在**装配期**报出来，而不是等用户点到。

## 维护空间

**是什么**：渠道在面板里的呈现形态。一级菜单放通用能力（仪表盘、账号总表、
API 密钥、用量、模型、日志、设置），每个渠道是一个独立维护空间，带自己的二级菜单。

**落地的样子**：WorkBuddy 渠道出现「签到 / 任务」，Grok 不会；Grok 出现「出口」，
WorkBuddy 不会。菜单项由该渠道声明的能力决定。

## 适配器（Adapter）与接缝（Seam）

**是什么**：每种账号类型一个适配器，把「对话、查额度、续期、导入凭据」翻译成
对应上游的协议。`internal/adapter` 是接缝，`internal/channel` 是它的词汇。

**边界**：能力接口是**细分**的——实现哪个接口就是具备哪个能力。`grok_console`
不实现 `Refresher`，调用方断言失败就走「不支持」分支。这比返回 `ErrNotSupported`
更早暴露问题，因为它把错误从运行期提到了装配期。

## 命名约定

| 位置 | 形式 | 例 |
| --- | --- | --- |
| 渠道 ID | 小写、连字符、带渠道族前缀 | `wbp-cn`、`wbp-global`、`grok` |
| 账号类型 | 小写下划线，渠道族作前缀 | `wbp`、`grok_web`、`grok_build` |
| 能力 | `域.动作` | `account.refresh`、`ops.checkin` |
